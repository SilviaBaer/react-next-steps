const Wrapper = props => {
    return props.children;
};

export default Wrapper;

//children prop hold all the contents you are passing beween the <> of your custom component
//ready to be exported  as <Wrapper><Wrapper/> and substitute the div component (div soup)
